# Everything under /static/ is content-hashed by the CRA build —
# safe to cache forever; a new deploy ships new filenames.
<IfModule mod_headers.c>
  Header set Cache-Control "public, max-age=31536000, immutable"
</IfModule>
